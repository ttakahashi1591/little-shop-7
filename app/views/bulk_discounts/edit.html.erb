<%= form_with model: @bulk_discount, url: "/merchants/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}", data: {turbo: false} do |f| %>
  Threshold: <%= f.text_field :threshold %>
  Discount: <%= f.text_field :discount %>
  <%= f.submit "Submit" %>
<% end %>
