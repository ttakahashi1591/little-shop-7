<h1>Disount ID: <%= @bulk_discount.id %></h1>
<ul>
  <li>Threshold: <%= @bulk_discount.threshold %></li>
  <li>Discount: <%= @bulk_discount.discount %></li>
</ul>
<%= link_to "Edit", "/merchants/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}/edit"%>